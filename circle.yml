machine:
  services:
    - docker

dependencies:
  override:
    - docker build -t surge-api api/ 

test:
  override:
    - docker run -p 9000:9000  -t -i surge-api revel run github.com/velomatrix/surge/api
    - curl --retry 10 --retry-delay 5 -v http://localhost:9000

