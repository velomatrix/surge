dependencies:
  override:
    - go get github.com/revel/revel
    - go get github.com/revel/cmd/revel

test:
  override:
    - go env
    - which revel
    - which go
    - ls -la
    - pwd
    - mkdir -p ~/.go_workspace/src/github.com/velomatrix
    - ln -s . ~/.go_workspace/src/github.com/velomatrix/surge
    - ls /home/ubuntu/.go_workspace/src/github.com/velomatrix/surge/api
    - revel test github.com/velomatrix/surge/api

